import { BillingFilterPipe } from './billing-filter-pipe';

describe('BillingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BillingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
